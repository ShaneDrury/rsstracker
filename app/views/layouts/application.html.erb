<!DOCTYPE html>
<html>
<head>
  <%= favicon_link_tag asset_path('logo-small.png') %>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%= action_cable_meta_tag %>
  <title><%= content_for?(:title) ? yield(:title) : "RSS Tracker" %></title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_pack_tag 'application' %>
  <style>
    html, body {
      height: 100%;
    }
  </style>
</head>
<body>
<%= yield %>
<%= yield :react %>
</body>
</html>
